# GENERATED by tools/of_manifest.py -- do not edit the `derived:` blocks by hand.
# The `classification:`/`brae_status:`/`note:` fields are CURATED and live in that script.
solver: interFoam
openfoam_root: /usr/lib/openfoam/openfoam2412

components:
  interFoam_main:
    openfoam:
      symbol: main
      file: applications/solvers/multiphase/interFoam/interFoam.C
    classification: HOST_ONLY
    brae_status: REIMPLEMENT
    brae_reference: src/applications/solvers/interFoam/interFoam_cpp.cu
    brae_target: src/applications/solvers/interFoam/interFoam.cu
    validation: "tests/interfoam_tutorials_vs_openfoam.sh -- damBreak first (the canonical case), then capillaryRise (surface tension IS the answer there, so a missing sigma cannot hide), then damBreakWithObstacle in 3D."
    note: "The time loop is PIMPLE and brae_pimpleFoam already runs it. Order per interFoam.C:151-168: alphaControls, alphaEqnSubCycle, mixture.correct(), UEqn, then the pressure corrector loop, then turbulence. adjustTimeStep/maxCo is REFUSED by brae_pimpleFoam today and interFoam needs it, so that lands here or before."

  interFoam_alphaEqn:
    openfoam:
      symbol: alphaEqn
      file: applications/solvers/multiphase/VoF/alphaEqn.H
    classification: SHARED_NUMERICAL
    brae_status: REIMPLEMENT
    brae_reference: src/applications/solvers/interFoam/alphaEqn_cpp.cu
    brae_target: src/applications/solvers/interFoam/alphaEqn.cu
    validation: "Boundedness is an ASSERTION, not a tolerance: 0 <= alpha <= 1 exactly, every cell, every sub-cycle. A VoF gate that only checks agreement can pass while the field goes unbounded and is then clipped."
    note: "cAlpha interface compression enters as div(phirb,alpha) with phir = cAlpha*|phi/magSf|. Only Euler and CrankNicolson ddt are accepted (alphaEqn.H:44-50 FatalErrors otherwise), and CrankNicolson is refused when sub-cycling."

  interFoam_MULES:
    openfoam:
      symbol: "MULES::limiter"
      file: src/finiteVolume/fvMatrices/solvers/MULES/MULES.C
    classification: SHARED_NUMERICAL
    brae_status: REIMPLEMENT
    brae_reference: src/finiteVolume/fvMatrices/solvers/MULES/mules_cpp.cu
    brae_target: src/finiteVolume/fvMatrices/solvers/MULES/device_mules.cu
    validation: "Against OpenFOAM's own limiter field, face by face. The oracle needs an instrumented MULES (see the of-instrument skill) because the limiter is never written by a stock run."
    note: "THE LARGEST SINGLE PIECE, and the least brae-like. MULES is an EXPLICIT, ITERATIVE, bound-preserving flux limiter -- not a matrix assembly -- so almost none of the fvm:: machinery the other solvers share applies to it. Budget it as its own stage."

  interFoam_CMULES:
    openfoam:
      symbol: "MULES::correct"
      file: src/finiteVolume/fvMatrices/solvers/MULES/CMULESTemplates.C
    classification: SHARED_NUMERICAL
    brae_status: REIMPLEMENT
    brae_reference: src/finiteVolume/fvMatrices/solvers/MULES/mules_cpp.cu
    brae_target: src/finiteVolume/fvMatrices/solvers/MULES/device_mules.cu
    validation: Shares the MULES gate; the semi-implicit path is a separate arm of it.
    note: "The semi-implicit variant, used when MULESCorr is set. Separate from the explicit limiter above and selected per case."

  interFoam_interfaceProperties:
    openfoam:
      symbol: interfaceProperties
      file: src/transportModels/interfaceProperties/interfaceProperties.C
    classification: MODEL
    brae_status: REIMPLEMENT
    brae_reference: src/transportModels/interfaceProperties/interface_properties_cpp.cu
    brae_target: src/transportModels/interfaceProperties/device_interface_properties.cu
    validation: "Curvature K_ against OpenFOAM's own, which a stock run never writes -- so this needs an instrumented interfaceProperties. Curvature is where a VoF port diverges invisibly."
    note: "K_ = -div(nHatf) where nHatf = (gradAlphaf/(|gradAlphaf| + deltaN)) & Sf (interfaceProperties.C:141-153), plus alphaContactAngle correction on wall patches. surfaceTensionForce() = sigma*K_*snGrad(alpha1)."

  interFoam_twoPhaseMixture:
    openfoam:
      symbol: twoPhaseMixture
      file: src/transportModels/twoPhaseMixture/twoPhaseMixture/twoPhaseMixture.C
    classification: MODEL
    brae_status: REIMPLEMENT
    brae_reference: src/transportModels/twoPhaseMixture/two_phase_mixture_cpp.cu
    brae_target: src/transportModels/twoPhaseMixture/device_two_phase_mixture.cu
    validation: "rho and mu as alpha-weighted blends, against OpenFOAM's own fields at iteration 1."
    note: "Field plumbing rather than new numerics: rho = alpha1*rho1 + (1-alpha1)*rho2, same for mu."

  interFoam_immiscibleMixture:
    openfoam:
      symbol: immiscibleIncompressibleTwoPhaseMixture
      file: src/transportModels/immiscibleIncompressibleTwoPhaseMixture/immiscibleIncompressibleTwoPhaseMixture.C
    classification: MODEL
    brae_status: REIMPLEMENT
    brae_reference: src/transportModels/twoPhaseMixture/two_phase_mixture_cpp.cu
    brae_target: src/transportModels/twoPhaseMixture/device_two_phase_mixture.cu
    validation: Shares the twoPhaseMixture gate.
    note: Joins twoPhaseMixture and interfaceProperties into the one object interFoam.C holds.

  interFoam_UEqn:
    openfoam:
      symbol: UEqn
      file: applications/solvers/multiphase/interFoam/UEqn.H
    classification: SHARED_NUMERICAL
    brae_status: REIMPLEMENT
    brae_reference: src/applications/solvers/interFoam/interUEqn_cpp.cu
    brae_target: src/applications/solvers/interFoam/interUEqn.cu
    validation: "Against OpenFOAM's own assembled matrix, as the rhoSimpleFoam momentum gate is."
    note: "fvm::ddt(rho,U) + fvm::div(rhoPhi,U) + MRF.DDt(rho,U) + turbulence->divDevRhoReff(rho,U), with the momentum predictor's source reconstructed from surfaceTensionForce() - ghf*snGrad(rho) - snGrad(p_rgh). divDevRhoReff is the COMPRESSIBLE overload brae already has."

  interFoam_pEqn:
    openfoam:
      symbol: pEqn
      file: applications/solvers/multiphase/interFoam/pEqn.H
    classification: SHARED_NUMERICAL
    brae_status: REIMPLEMENT
    brae_reference: src/applications/solvers/interFoam/interPEqn_cpp.cu
    brae_target: src/applications/solvers/interFoam/interPEqn.cu
    validation: "Against OpenFOAM's own p_rgh matrix and the reconstructed flux."
    note: "A DIFFERENT PRESSURE EQUATION from anything ported: laplacian(rAUf, p_rgh) == div(phiHbyA), with phig = (surfaceTensionForce() - ghf*snGrad(rho))*rAUf*magSf added to phiHbyA first. p_rgh, not p. constrainHbyA/adjustPhi/constrainPressure are already ported and reused."

  interFoam_vanLeer:
    openfoam:
      symbol: vanLeer
      file: src/finiteVolume/interpolation/surfaceInterpolation/limitedSchemes/vanLeer/vanLeer.C
    classification: SHARED_NUMERICAL
    brae_status: REIMPLEMENT
    brae_reference: src/finiteVolume/interpolation/surfaceInterpolation/limitedSchemes/limitedSchemes_cpp.cuh
    brae_target: src/cuda/device_fvm.cu
    validation: "Against OpenFOAM's own face weights, as the other limited schemes are."
    note: "EVERY interFoam tutorial uses `div(phi,alpha) Gauss vanLeer`. brae has limitedLinear/V, vanAlbada, LUST, linearUpwind/V -- not vanLeer. Small, and blocking."

  interFoam_alphaCourantNo:
    openfoam:
      symbol: alphaCourantNo
      file: applications/solvers/multiphase/VoF/alphaCourantNo.H
    classification: CONFIGURATION
    brae_status: REIMPLEMENT
    brae_reference: src/applications/solvers/interFoam/interFoam_cpp.cu
    brae_target: src/applications/solvers/interFoam/interFoam.cu
    validation: Part of the end-to-end tutorial gate; the interface Courant number sets the step.
    note: "maxAlphaCo drives the adaptive time step alongside maxCo. Needs adjustTimeStep, which brae_pimpleFoam refuses today."

  interFoam_createFields:
    openfoam:
      symbol: createFields
      file: applications/solvers/multiphase/interFoam/createFields.H
    classification: CONFIGURATION
    brae_status: REIMPLEMENT
    brae_reference: src/applications/solvers/interFoam/interCreateFields_cpp.cu
    brae_target: src/applications/solvers/interFoam/interCreateFields.cu
    validation: "Field-by-field against OpenFOAM's own written state, as rhoCreateFields is."
    note: "alpha1, p_rgh, gh/ghf from the gravity field, rhoPhi. g and hRef come from constant/g and constant/hRef, neither of which brae reads today."

validation_cases:
  damBreak:
    path: /usr/lib/openfoam/openfoam2412/tutorials/multiphase/interFoam/laminar/damBreak/damBreak
    derived_selection_sites: 31
    derived_unresolved: 0
    derived_required_implementations:
      - {category: boundary, keyword: empty, resolves_to: emptyPolyPatch}
      - {category: boundary, keyword: fixedFluxPressure, resolves_to: fixedFluxPressureFvPatchScalarField}
      - {category: boundary, keyword: inletOutlet, resolves_to: inletOutlet}
      - {category: boundary, keyword: noSlip, resolves_to: noSlipFvPatchVectorField}
      - {category: boundary, keyword: pressureInletOutletVelocity, resolves_to: pressureInletOutletVelocityFvPatchVectorField}
      - {category: boundary, keyword: totalPressure, resolves_to: totalPressureFvPatchScalarField}
      - {category: boundary, keyword: zeroGradient, resolves_to: zeroGradient}
      - {category: linear-solver, keyword: DIC, resolves_to: DICPreconditioner}
      - {category: linear-solver, keyword: PCG, resolves_to: PCG}
      - {category: linear-solver, keyword: smoothSolver, resolves_to: smoothSolver}
      - {category: linear-solver, keyword: symGaussSeidel, resolves_to: symGaussSeidelSmoother}
      - {category: scheme, keyword: Euler, resolves_to: EulerDdtScheme}
      - {category: scheme, keyword: corrected, resolves_to: correctedSnGrad}
      - {category: scheme, keyword: linear, resolves_to: linear}

  damBreak:
    path: /usr/lib/openfoam/openfoam2412/tutorials/multiphase/interFoam/RAS/damBreak/damBreak
    derived_selection_sites: 52
    derived_unresolved: 0
    derived_required_implementations:
      - {category: boundary, keyword: calculated, resolves_to: calculated}
      - {category: boundary, keyword: empty, resolves_to: emptyPolyPatch}
      - {category: boundary, keyword: epsilonWallFunction, resolves_to: epsilonWallFunctionFvPatchScalarField}
      - {category: boundary, keyword: fixedFluxPressure, resolves_to: fixedFluxPressureFvPatchScalarField}
      - {category: boundary, keyword: inletOutlet, resolves_to: inletOutlet}
      - {category: boundary, keyword: kqRWallFunction, resolves_to: kqRWallFunction}
      - {category: boundary, keyword: noSlip, resolves_to: noSlipFvPatchVectorField}
      - {category: boundary, keyword: nutkWallFunction, resolves_to: nutkWallFunctionFvPatchScalarField}
      - {category: boundary, keyword: pressureInletOutletVelocity, resolves_to: pressureInletOutletVelocityFvPatchVectorField}
      - {category: boundary, keyword: totalPressure, resolves_to: totalPressureFvPatchScalarField}
      - {category: boundary, keyword: zeroGradient, resolves_to: zeroGradient}
      - {category: linear-solver, keyword: DIC, resolves_to: DICPreconditioner}
      - {category: linear-solver, keyword: PCG, resolves_to: PCG}
      - {category: linear-solver, keyword: smoothSolver, resolves_to: smoothSolver}
      - {category: linear-solver, keyword: symGaussSeidel, resolves_to: symGaussSeidelSmoother}
      - {category: scheme, keyword: Euler, resolves_to: EulerDdtScheme}
      - {category: scheme, keyword: linear, resolves_to: linear}
      - {category: scheme, keyword: orthogonal, resolves_to: orthogonalSnGrad}
      - {category: turbulence, keyword: kEpsilon, resolves_to: kEpsilon}

  capillaryRise:
    path: /usr/lib/openfoam/openfoam2412/tutorials/multiphase/interFoam/laminar/capillaryRise
    derived_selection_sites: 26
    derived_unresolved: 0
    derived_required_implementations:
      - {category: boundary, keyword: constantAlphaContactAngle, resolves_to: constantAlphaContactAngleFvPatchScalarField}
      - {category: boundary, keyword: empty, resolves_to: emptyPolyPatch}
      - {category: boundary, keyword: fixedFluxPressure, resolves_to: fixedFluxPressureFvPatchScalarField}
      - {category: boundary, keyword: fixedValue, resolves_to: fixedValue}
      - {category: boundary, keyword: inletOutlet, resolves_to: inletOutlet}
      - {category: boundary, keyword: noSlip, resolves_to: noSlipFvPatchVectorField}
      - {category: boundary, keyword: pressureInletOutletVelocity, resolves_to: pressureInletOutletVelocityFvPatchVectorField}
      - {category: boundary, keyword: zeroGradient, resolves_to: zeroGradient}
      - {category: linear-solver, keyword: DIC, resolves_to: DICPreconditioner}
      - {category: linear-solver, keyword: PCG, resolves_to: PCG}
      - {category: linear-solver, keyword: smoothSolver, resolves_to: smoothSolver}
      - {category: linear-solver, keyword: symGaussSeidel, resolves_to: symGaussSeidelSmoother}
      - {category: scheme, keyword: Euler, resolves_to: EulerDdtScheme}
      - {category: scheme, keyword: corrected, resolves_to: correctedSnGrad}
      - {category: scheme, keyword: linear, resolves_to: linear}

